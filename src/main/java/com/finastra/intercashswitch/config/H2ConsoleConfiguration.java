package com.finastra.intercashswitch.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class H2ConsoleConfiguration {

	/*
	 * @Bean ServletRegistrationBean<Servlet> h2servletRegistration() {
	 * ServletRegistrationBean<Servlet> registrationBean = new
	 * ServletRegistrationBean<Servlet>(new WebServlet());
	 * registrationBean.addUrlMappings("/h2-console/*"); return registrationBean; }
	 */
}